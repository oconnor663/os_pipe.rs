[package]
name = "swap"
version = "0.1.0"
authors = ["Jack O'Connor"]
description = "swap stdout and stderr for (Windows compatible) testing"
license = "MIT"

[dependencies]
os_pipe = { path = "../../" }
